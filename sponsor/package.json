{
  "name": "@agent-passport/sponsor",
  "version": "0.1.0",
  "private": true,
  "description": "Fee-payer sponsor service: validates a passport-write transaction, co-signs as fee payer, pays rent/fees, and submits. Lets an embedded owner key hold no SOL.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts src/server.ts --format esm --dts --clean --sourcemap",
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-passport/sdk": "*",
    "@solana/web3.js": "^1.98.0",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
